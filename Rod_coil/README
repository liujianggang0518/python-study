%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
YPlm=prepare(N1,BNC,N3C,N2C)                         %准备工作，在进行球调和变换之前要使用prepare，请放在你的循环外！！！！！！


qy=real2spherical_r2c(N1,N3C,BNC,N2C,qB0,YPlm)       %实到复

qB0=spherical2real_c2r(N1,N3C,BNC,N2C,qy,YPlm)       %复到实

BNC=16;                                              %[0,BNC]球调和函数中l的范围:l=0,1,2,...,16
N2C=BNC;                                             %经度方向sita[o,pi]的打点数(thetan)
N1=BNC+1;                                            %球调和系数中l的个数，表示进行  r2c 变换后系数矩阵的行数
N3C=2*N2C+1;                                         %维度方向phi [0,2*pi]的打点数,也表示m的个数，即进行r2c变换后系数矩阵的列数(phin)


qB0；                                                %表示进行  r2c  实函数，实到复的输入函数，矩阵大小为 N2C*（2*N2C+1）
qy;                                                  %表示进行  r2c 变换后系数矩阵，矩阵大小为 （N2C+1）*（2*N2C+1）
